# سجل التغييرات

جميع التغييرات المهمة في هذا المشروع موثّقة هنا، الأحدث أولاً.

---

## [1.1.0] — 2026-09-01

### أُضيف

* **مجلد عام باسم التطبيق (أسلوب واتساب).** الملفات تُحفظ الآن في مجلد
  يراه المستخدم في مدير الملفات ويبقى بعد إلغاء تثبيت التطبيق:

  ```
  Documents/فواتير الكهرباء/
  ├── الفواتير/
  └── النسخ الاحتياطي/
  ```

* **طبقة أصليّة بـ Kotlin** (`PublicStoragePlugin.kt`) تكتب عبر
  `MediaStore` + `ContentResolver`، لأن أندرويد 10+ يمنع الكتابة
  المباشرة للذاكرة المشتركة، ولا توجد حزمة Dart تسمح باختيار **مجلد
  فرعي داخل Documents**. تفاصيل كاملة في
  [`docs/STORAGE_AND_PERMISSIONS.md`](docs/STORAGE_AND_PERMISSIONS.md).

* **درج جانبي** يُفتح من اليمين تلقائياً في الواجهة العربية، ويضم:
  الرئيسية، المشتركون، فاتورة جديدة، أرشيف الفواتير، الإعدادات،
  المطوّر والتواصل — مع تظليل ذهبي للعنصر النشط.

* **صفحة المطوّر والتواصل** (`/about`): واتساب `+967 770 941 666`،
  الموقع `alabbasi.uk`، اتصال هاتفي. ضغطة تفتح التطبيق المناسب، وضغطة
  طويلة تنسخ.

* **زرّ مستقل للنسخ الاحتياطي** «حفظ نسخة في مجلد التطبيق»، منفصل عن
  زرّ المشاركة، مع نافذة توضيحية عند رفض الصلاحية وزرّ يفتح الإعدادات.

* **أسماء ملفات وصفية:**

  | النوع | الصيغة |
  |---|---|
  | فاتورة | `اسم المشترك - YYYY-MM-DD - رقم الفاتورة.pdf` |
  | نسخة احتياطية | `نسخة احتياطية - YYYY-MM-DD - HHmm.json` |

  مع تنقية الأحرف الممنوعة، ومنع الأسماء الفارغة، وحدّ طول 80 حرفاً.

* **10 اختبارات جديدة** لتنقية أسماء الملفات وتوليدها.

* **توثيق:** `docs/STORAGE_AND_PERMISSIONS.md` جديد، و`README.md`
  و`docs/INVOICE_FORENSICS.md` أُعيد كتابتهما بالكامل.

### أُصلح

* **اسم التطبيق كان يظهر "My App"** على الجهاز، لأن `android:label` كان
  نصاً حرفياً تعيد أدوات البناء الآلية كتابته. نُقل الاسم إلى مورد نصّي
  `@string/app_name` = «فواتير الكهرباء».

* **صلاحية تخزين غير ضرورية.** أندرويد 10+ لا يحتاج أي صلاحية لملفات
  ينشئها التطبيق نفسه، فحُدِّد الطلب على أندرويد 9 وأقل عبر
  `android:maxSdkVersion="28"`. طلب صلاحية لا لزوم لها يُضعف ثقة
  المستخدم وقد يُعطّل مراجعة المتجر.

* **`url_launcher` كان يفشل بصمت** على أندرويد 11+ (`canLaunchUrl`
  تُرجع `false`) بسبب قيود ظهور الحِزم. أُضيفت نوايا `https` و`tel`
  و`SEND` في `<queries>`، مع `LaunchMode.externalApplication` حتى لا
  يُفتح الرابط داخل التطبيق.

* **خطأ بناء `ambiguous_import`** — كانت `invoiceFileName` معرَّفة في
  ملفين. أُزيلت النسخة الأضعف من `invoice_pdf.dart`.

### تغيّر

* توحيد نص المطوّر «تطوير م. معين العباسي» وكل ثوابت الهوية في مصدر
  واحد `lib/core/app_info.dart`.
* رفع الإصدار إلى `1.1.0+2`.

### الفاتورة — لم تُمسّ

التعديل الوحيد في `invoice_pdf.dart` كان إزالة دالة مكرّرة كانت تمنع
البناء، مع إعادة تنسيق آلية. **لا مسّ بالتخطيط أو الشعار أو الألوان أو
الهوامش.** التحقّق بعد كل التعديلات:

| المقياس | النتيجة |
|---|---|
| حجم الملف | 95,408 بايت — مطابق بايت-ببايت للنسخة المعتمدة |
| مقاس الصفحة | 297.0 × 156.8 مم |
| مقاس الخط في كتلة البيانات | 23.14 (قيمة واحدة ⇒ لا تصغير) |
| محاذاة النقطتين | 6/6 بانحراف 0.000 بكسل |

---

## [1.0.0] — 2026-08-31

### أُضيف

* التحويل الكامل من تطبيق الويب (Next.js PWA) إلى تطبيق أندرويد
  بـ Flutter، مع بنية Feature-First Clean Architecture.
* قاعدة بيانات محليّة على الجهاز (Drift/SQLite) مع مستودع بيانات
  ونسخ احتياطي بصيغة JSON متوافقة مع النسخة السابقة.
* حساب الفواتير، إدارة المشتركين، الأرشيف، لوحة التحكم والإحصائيات.
* توليد الفاتورة PDF مطابقةً للأصل — التفاصيل الكاملة في
  [`docs/INVOICE_FORENSICS.md`](docs/INVOICE_FORENSICS.md).

### أُصلح

* **إصلاحات الفاتورة الحاكمة:** توحيد لون المبلغ المستحق مع لون
  العنوان، وإنزال التسطير ليكون *تحت* العنوان لا خلاله، وإنزال الخط
  الفاصل تحت المبلغ المستحق، ومنع تداخل الأسطر عبر اشتقاق كل
  الإحداثيات في سلسلة واحدة.
* **تداخل النقطتين واختفاء الأرقام** — أُعيد بناء كتلة البيانات
  بتخطيط `pw.Table` مرن يقيس نفسه.
* **مصيدة `FittedBox`** التي كانت تُصغّر النص على محور الارتفاع أيضاً.

### تغيّر

* تقليص حجم الحزمة من **65 MB** إلى **~10 MB** عبر
  `--split-per-abi` و`--obfuscate` وحصر ملفات الموارد على `ar,en`.
* تعريف التطبيق `com.abbasisoft.billing` وتوقيع الإصدار وأيقونات
  التشغيل.

---

## الإصدارات

| الإصدار | التحميل |
|---|---|
| 1.1.0 | [صفحة الإصدار](https://github.com/MoTechSys/electricity-billing-flutter/releases/tag/v1.1.0) |
| 1.0.0 | [صفحة الإصدار](https://github.com/MoTechSys/electricity-billing-flutter/releases/tag/v1.0.0) |

### أي ملف أُنزّل؟

| الملف | الأجهزة |
|---|---|
| `app-arm64-v8a-release.apk` | معظم الهواتف الحديثة — **الموصى به** |
| `app-armeabi-v7a-release.apk` | هواتف أقدم 32-بت |
| `app-x86_64-release.apk` | المحاكيات |
